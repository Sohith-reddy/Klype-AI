<!DOCTYPE html>
<html>
<head>
  <title>Admin Login</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <style>
    /* Full screen black background with subtle purple glow */
    body {
      margin: 0;
      height: 100vh;
      background: #0b0014;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Poppins', sans-serif;
      color: #ddd;
      overflow: hidden;
    }

    /* Purple animated gradient circle behind the form */
    body::before {
      content: "";
      position: absolute;
      width: 450px;
      height: 450px;
      background: linear-gradient(270deg, #7e22ce, #5b21b6, #8b5cf6);
      border-radius: 50%;
      filter: blur(120px);
      animation: pulse 8s ease infinite;
      z-index: -1;
    }

    @keyframes pulse {
      0%, 100% {
        transform: translateX(0) translateY(0);
        opacity: 0.7;
      }
      50% {
        transform: translateX(30px) translateY(15px);
        opacity: 0.9;
      }
    }

    form {
      position: relative;
      background: #1f003f;
      padding: 50px 40px;
      border-radius: 20px;
      box-shadow: 0 0 20px #7e22ce;
      width: 350px;
      z-index: 1;
      text-align: center;
    }

    h4 {
      font-weight: 700;
      font-size: 28px;
      margin-bottom: 40px;
      color: #d8b4fe;
      letter-spacing: 1.2px;
      text-shadow: 0 0 10px #a78bfa;
    }

    input.form-control {
      background: #2e026d;
      border: none;
      border-radius: 10px;
      color: #eee;
      padding: 15px 20px;
      margin-bottom: 25px;
      font-size: 16px;
      box-shadow: inset 0 0 8px #7e22ce;
      transition: background 0.3s ease, box-shadow 0.3s ease;
    }
    input.form-control::placeholder {
      color: #c4b5fd;
    }
    input.form-control:focus {
      background: #5b21b6;
      box-shadow: 0 0 15px #c084fc;
      color: white;
      outline: none;
    }

    button.btn-primary {
      background: #7c3aed;
      border: none;
      border-radius: 12px;
      padding: 15px 0;
      font-size: 18px;
      font-weight: 600;
      letter-spacing: 1px;
      box-shadow: 0 6px 15px #7c3aedcc;
      transition: background 0.3s ease, box-shadow 0.3s ease;
      width: 100%;
      cursor: pointer;
    }
    button.btn-primary:hover {
      background: #9333ea;
      box-shadow: 0 8px 25px #9333eacc;
    }
  </style>
</head>
<body>
  <form method="POST" action="/admin/login" autocomplete="off" spellcheck="false">
    <h4>Admin Login</h4>
    <input type="text" name="username" class="form-control" placeholder="Username" required />
    <input type="password" name="password" class="form-control" placeholder="Password" required />
    <button class="btn btn-primary" type="submit">Login</button>
  </form>
</body>
</html>
